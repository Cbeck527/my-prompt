[package]
name = "my-prompt"
version = "0.0.8"
edition = "2024"
authors = ["Chris Becker <chris@becker.am>"]
description = "My ultra-fast shell prompt based on `prmt` by Ivan Zakharchanka"
license = "MIT"
repository = "https://github.com/cbeck527/my-prompt"
homepage = "https://github.com/cbeck527/my-prompt"
readme = "README.md"
keywords = ["prompt", "shell", "terminal", "cli", "performance"]
categories = ["command-line-utilities", "command-line-interface"]

[lib]
name = "my_prompt"
path = "src/lib.rs"

[[bin]]
name = "my-prompt"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6", features = ["derive", "env"] }
gix = { version = "0.85", default-features = false, features = ["max-performance-safe", "sha1", "status", "revision"] }
rayon = "1"
dirs = "6"
thiserror = "2"
anyhow = "1"
bitflags = "2"
chrono = "0.4"
whoami = "2.1"
serde_json = "1.0.150"
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }
regex = "1"
serial_test = "3"
tempfile = "3"

[lints.clippy]
pedantic = { level = "deny", priority = -1 }

[lints.rust]
warnings = "deny"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
strip = true
panic = "abort"
overflow-checks = false

[profile.release.build-override]
opt-level = 0
codegen-units = 16
